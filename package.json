{
  "name": "monorepo",
  "private": true,
  "packageManager": "pnpm@10.3.0",
  "scripts": {
    "build:native": "pnpm --filter=@unplugin-sri/algorithm-sha run build",
    "build:node": "pnpm --filter=unplugin-sri run build"
  }
}
