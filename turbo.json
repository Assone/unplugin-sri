{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist", "*.node"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    },
    "lint": {}
  }
}